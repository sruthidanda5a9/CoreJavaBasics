package Thread;

public class ThreadJoin {
	/*
	 * here in the name it self in the current thread if we found  [anotherthread.join()] means 
	 * our current thread execution will be stopped and it will be added to anotherthread.
	 * after the completion of execution of anotherthread our current thread will added to that.
	 * 
	 * see
	 * 
	 * Thread t = new Thread();
	 * t.join();
	 * 
	 * if we found this in our current thread. current thread execution will be stopped and "t" is started execution.
	 * curent thread will be appended to the "T".
	 * 
	 */
	
	

}
